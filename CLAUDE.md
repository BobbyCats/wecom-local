# Claude Instructions

Use `skills/claude/SKILL.md` when working with this repository from Claude.

The Rust CLI is the implementation source of truth. Agent-specific files should
stay thin and should not duplicate runtime access logic.
